{
  "name": "carshop",
  "version": "1.0.0",
  "private": true,
  "description": "Сборка всего проекта одной командой: фронтенд собирается в статику, сервер её отдаёт",
  "scripts": {
    "build": "cd server && npm install && cd ../client/carshop && npm install && npm run build",
    "start": "node server/app.js",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client/carshop && npm run dev"
  },
  "engines": {
    "node": ">=20"
  }
}
